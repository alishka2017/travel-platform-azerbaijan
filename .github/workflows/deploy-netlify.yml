name: Deploy to Netlify

on:
  push:
    branches: [main, master, cms-integration]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci
        env:
          NPM_CONFIG_LEGACY_PEER_DEPS: true

      - name: Build with Next.js
        run: npx next build
        env:
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}

      - name: Deploy to Netlify
        # TODO: Replace with secrets.NETLIFY_SITE_ID after updating GitHub secret
        # Using local state.json ID for testing: 42ea6c58-bb48-4cf7-9f36-1e923fc38243
        run: npx netlify deploy --prod --site 42ea6c58-bb48-4cf7-9f36-1e923fc38243 --dir .next
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}